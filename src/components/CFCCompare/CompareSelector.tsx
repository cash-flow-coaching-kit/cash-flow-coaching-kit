import React, { ReactElement } from "react"

/**
 * Selector to choose between different canvases
 *
 * @export
 * @returns {ReactElement}
 */
export default function CompareSelector(): ReactElement {
	return <div>Selector</div>
}
